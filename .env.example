# Copiar a .env y ajustar en la PC del cliente (no commitear .env).
# Los mismos valores deben usarse en la pantalla "Configurar conexión" de la app.

POSTGRES_PORT=5432
POSTGRES_DB=fitness_training
POSTGRES_USER=postgres
POSTGRES_PASSWORD=cambiar_en_produccion
